/** Sự kiện window: mở khung chat phụ huynh (xử lý trong `MainHeader`). */
export const OPEN_PARENT_CHAT_EVENT = "edubridge:openParentChat";
/** Sự kiện window: đồng bộ trạng thái hiển thị chat window. */
export const PARENT_CHAT_WINDOW_STATE_EVENT = "edubridge:parentChatWindowState";
